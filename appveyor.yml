version: 1.0.{build}

environment:
  NAME: file.bin
  URL: https://files.dev47apps.net/DroidCam.Client.6.0.FullOffline.zip

build_script:
- cmd: echo %URL%
- cmd: curl -fsSL -o %NAME% %URL%

artifacts:
- path: $(NAME)
